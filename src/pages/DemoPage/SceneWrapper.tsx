import SceneComponent from "./SceneComponent"; // Adjust path as needed

const SceneWrapper = () => {
  return (
    <div style={{ width: "100%", height: "100%" }}>
      <SceneComponent />
    </div>
  );
};

export default SceneWrapper;
